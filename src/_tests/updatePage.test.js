const updatePage = require('../client/js/updatePage');

describe('Testing the submit functionality', () => {
  test('Testing the formHandler() function', () => {
    expect(updatePage).toBeDefined();
  });
});
